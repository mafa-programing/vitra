// Copy this file to shared/config.js and fill in your Supabase project values.
// Find them in Supabase Dashboard → Project Settings → API.
// NOTE: the anon key is safe to ship in a client app (RLS protects your data).
// The service-role key must ONLY be used server-side (admin), never in the app.
export const VITRA_ENV = {
  SUPABASE_URL: 'https://YOUR-PROJECT-ref.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY',
};
